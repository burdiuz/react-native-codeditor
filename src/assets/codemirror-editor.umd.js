// Generated by scripts/copy-assets.js — do not edit directly.
// Source: node_modules/@actualwave/codemirror-package/dist/
// Plain IIFE bundle (no import/export) for loading via <script> in Android WebView.
// Exposes: window.CodeMirrorEditor = { configure, createEditor, requireAsyncModule, registerExtension }
(function (global) {
'use strict';
// Injected during build with the full module map: { [packageName]: { file, core?, bundle? } }
const modules = {"crelt":{"file":"crelt","core":true,"bundle":"_core"},"style-mod":{"file":"style-mod","core":true,"bundle":"_core"},"w3c-keyname":{"file":"w3c-keyname","core":true,"bundle":"_core"},"@marijn/find-cluster-break":{"file":"@marijn_find-cluster-break","core":true,"bundle":"_core"},"@lezer/common":{"file":"@lezer_common","core":true,"bundle":"_core"},"@lezer/highlight":{"file":"@lezer_highlight","core":true,"bundle":"_core"},"@lezer/lr":{"file":"@lezer_lr","core":true,"bundle":"_core"},"@codemirror/state":{"file":"@codemirror_state","core":true,"bundle":"_core"},"@codemirror/view":{"file":"@codemirror_view","core":true,"bundle":"_core"},"@codemirror/language":{"file":"@codemirror_language","core":true,"bundle":"_core"},"@codemirror/commands":{"file":"@codemirror_commands","core":true,"bundle":"_core"},"@codemirror/autocomplete":{"file":"@codemirror_autocomplete","core":true,"bundle":"_core"},"@codemirror/search":{"file":"@codemirror_search","core":true,"bundle":"_core"},"@codemirror/lint":{"file":"@codemirror_lint","core":true,"bundle":"_core"},"codemirror":{"file":"codemirror"},"@codemirror/collab":{"file":"@codemirror_collab"},"@codemirror/language-data":{"file":"@codemirror_language-data"},"@codemirror/merge":{"file":"@codemirror_merge"},"@codemirror/theme-one-dark":{"file":"@codemirror_theme-one-dark"},"@uiw/codemirror-themes":{"file":"@uiw_codemirror-themes"},"@uiw/codemirror-theme-androidstudio":{"file":"@uiw_codemirror-theme-androidstudio"},"@uiw/codemirror-theme-andromeda":{"file":"@uiw_codemirror-theme-andromeda"},"@uiw/codemirror-theme-atomone":{"file":"@uiw_codemirror-theme-atomone"},"@uiw/codemirror-theme-aura":{"file":"@uiw_codemirror-theme-aura"},"@uiw/codemirror-theme-basic":{"file":"@uiw_codemirror-theme-basic"},"@uiw/codemirror-theme-bbedit":{"file":"@uiw_codemirror-theme-bbedit"},"@uiw/codemirror-theme-copilot":{"file":"@uiw_codemirror-theme-copilot"},"@uiw/codemirror-theme-darcula":{"file":"@uiw_codemirror-theme-darcula"},"@uiw/codemirror-theme-dracula":{"file":"@uiw_codemirror-theme-dracula"},"@uiw/codemirror-theme-duotone":{"file":"@uiw_codemirror-theme-duotone"},"@uiw/codemirror-theme-eclipse":{"file":"@uiw_codemirror-theme-eclipse"},"@uiw/codemirror-theme-github":{"file":"@uiw_codemirror-theme-github"},"@uiw/codemirror-theme-material":{"file":"@uiw_codemirror-theme-material"},"@uiw/codemirror-theme-monokai":{"file":"@uiw_codemirror-theme-monokai"},"@uiw/codemirror-theme-nord":{"file":"@uiw_codemirror-theme-nord"},"@uiw/codemirror-theme-okaidia":{"file":"@uiw_codemirror-theme-okaidia"},"@uiw/codemirror-theme-solarized":{"file":"@uiw_codemirror-theme-solarized"},"@uiw/codemirror-theme-sublime":{"file":"@uiw_codemirror-theme-sublime"},"@uiw/codemirror-theme-tokyo-night":{"file":"@uiw_codemirror-theme-tokyo-night"},"@uiw/codemirror-theme-vscode":{"file":"@uiw_codemirror-theme-vscode"},"@uiw/codemirror-theme-xcode":{"file":"@uiw_codemirror-theme-xcode"},"@lezer/css":{"file":"@lezer_css"},"@lezer/go":{"file":"@lezer_go"},"@lezer/html":{"file":"@lezer_html"},"@lezer/java":{"file":"@lezer_java"},"@lezer/javascript":{"file":"@lezer_javascript"},"@lezer/json":{"file":"@lezer_json"},"@lezer/cpp":{"file":"@lezer_cpp"},"@lezer/lezer":{"file":"@lezer_lezer"},"@lezer/markdown":{"file":"@lezer_markdown"},"@lezer/php":{"file":"@lezer_php"},"@lezer/python":{"file":"@lezer_python"},"@lezer/rust":{"file":"@lezer_rust"},"@lezer/sass":{"file":"@lezer_sass"},"@lezer/xml":{"file":"@lezer_xml"},"@lezer/yaml":{"file":"@lezer_yaml"},"@codemirror/lang-angular":{"file":"@codemirror_lang-angular"},"@codemirror/lang-cpp":{"file":"@codemirror_lang-cpp"},"@codemirror/lang-css":{"file":"@codemirror_lang-css"},"@codemirror/lang-go":{"file":"@codemirror_lang-go"},"@codemirror/lang-html":{"file":"@codemirror_lang-html"},"@codemirror/lang-java":{"file":"@codemirror_lang-java"},"@codemirror/lang-javascript":{"file":"@codemirror_lang-javascript"},"@codemirror/lang-jinja":{"file":"@codemirror_lang-jinja"},"@codemirror/lang-json":{"file":"@codemirror_lang-json"},"@codemirror/lang-less":{"file":"@codemirror_lang-less"},"@codemirror/lang-lezer":{"file":"@codemirror_lang-lezer"},"@codemirror/lang-liquid":{"file":"@codemirror_lang-liquid"},"@codemirror/lang-markdown":{"file":"@codemirror_lang-markdown"},"@codemirror/lang-php":{"file":"@codemirror_lang-php"},"@codemirror/lang-python":{"file":"@codemirror_lang-python"},"@codemirror/lang-rust":{"file":"@codemirror_lang-rust"},"@codemirror/lang-sass":{"file":"@codemirror_lang-sass"},"@codemirror/lang-sql":{"file":"@codemirror_lang-sql"},"@codemirror/lang-vue":{"file":"@codemirror_lang-vue"},"@codemirror/lang-wast":{"file":"@codemirror_lang-wast"},"@codemirror/lang-xml":{"file":"@codemirror_lang-xml"},"@codemirror/lang-yaml":{"file":"@codemirror_lang-yaml"},"@actualwave/codemirror-lang-sksl":{"file":"@actualwave_codemirror-lang-sksl"},"@codemirror/legacy-modes/mode/apl":{"file":"@codemirror_legacy-modes_mode_apl"},"@codemirror/legacy-modes/mode/asciiarmor":{"file":"@codemirror_legacy-modes_mode_asciiarmor"},"@codemirror/legacy-modes/mode/asn1":{"file":"@codemirror_legacy-modes_mode_asn1"},"@codemirror/legacy-modes/mode/asterisk":{"file":"@codemirror_legacy-modes_mode_asterisk"},"@codemirror/legacy-modes/mode/brainfuck":{"file":"@codemirror_legacy-modes_mode_brainfuck"},"@codemirror/legacy-modes/mode/clike":{"file":"@codemirror_legacy-modes_mode_clike"},"@codemirror/legacy-modes/mode/clojure":{"file":"@codemirror_legacy-modes_mode_clojure"},"@codemirror/legacy-modes/mode/cmake":{"file":"@codemirror_legacy-modes_mode_cmake"},"@codemirror/legacy-modes/mode/cobol":{"file":"@codemirror_legacy-modes_mode_cobol"},"@codemirror/legacy-modes/mode/coffeescript":{"file":"@codemirror_legacy-modes_mode_coffeescript"},"@codemirror/legacy-modes/mode/commonlisp":{"file":"@codemirror_legacy-modes_mode_commonlisp"},"@codemirror/legacy-modes/mode/crystal":{"file":"@codemirror_legacy-modes_mode_crystal"},"@codemirror/legacy-modes/mode/css":{"file":"@codemirror_legacy-modes_mode_css"},"@codemirror/legacy-modes/mode/cypher":{"file":"@codemirror_legacy-modes_mode_cypher"},"@codemirror/legacy-modes/mode/d":{"file":"@codemirror_legacy-modes_mode_d"},"@codemirror/legacy-modes/mode/diff":{"file":"@codemirror_legacy-modes_mode_diff"},"@codemirror/legacy-modes/mode/dockerfile":{"file":"@codemirror_legacy-modes_mode_dockerfile"},"@codemirror/legacy-modes/mode/dtd":{"file":"@codemirror_legacy-modes_mode_dtd"},"@codemirror/legacy-modes/mode/dylan":{"file":"@codemirror_legacy-modes_mode_dylan"},"@codemirror/legacy-modes/mode/ebnf":{"file":"@codemirror_legacy-modes_mode_ebnf"},"@codemirror/legacy-modes/mode/ecl":{"file":"@codemirror_legacy-modes_mode_ecl"},"@codemirror/legacy-modes/mode/eiffel":{"file":"@codemirror_legacy-modes_mode_eiffel"},"@codemirror/legacy-modes/mode/elm":{"file":"@codemirror_legacy-modes_mode_elm"},"@codemirror/legacy-modes/mode/erlang":{"file":"@codemirror_legacy-modes_mode_erlang"},"@codemirror/legacy-modes/mode/factor":{"file":"@codemirror_legacy-modes_mode_factor"},"@codemirror/legacy-modes/mode/fcl":{"file":"@codemirror_legacy-modes_mode_fcl"},"@codemirror/legacy-modes/mode/forth":{"file":"@codemirror_legacy-modes_mode_forth"},"@codemirror/legacy-modes/mode/fortran":{"file":"@codemirror_legacy-modes_mode_fortran"},"@codemirror/legacy-modes/mode/gas":{"file":"@codemirror_legacy-modes_mode_gas"},"@codemirror/legacy-modes/mode/gherkin":{"file":"@codemirror_legacy-modes_mode_gherkin"},"@codemirror/legacy-modes/mode/go":{"file":"@codemirror_legacy-modes_mode_go"},"@codemirror/legacy-modes/mode/groovy":{"file":"@codemirror_legacy-modes_mode_groovy"},"@codemirror/legacy-modes/mode/haskell":{"file":"@codemirror_legacy-modes_mode_haskell"},"@codemirror/legacy-modes/mode/haxe":{"file":"@codemirror_legacy-modes_mode_haxe"},"@codemirror/legacy-modes/mode/http":{"file":"@codemirror_legacy-modes_mode_http"},"@codemirror/legacy-modes/mode/idl":{"file":"@codemirror_legacy-modes_mode_idl"},"@codemirror/legacy-modes/mode/javascript":{"file":"@codemirror_legacy-modes_mode_javascript"},"@codemirror/legacy-modes/mode/jinja2":{"file":"@codemirror_legacy-modes_mode_jinja2"},"@codemirror/legacy-modes/mode/julia":{"file":"@codemirror_legacy-modes_mode_julia"},"@codemirror/legacy-modes/mode/livescript":{"file":"@codemirror_legacy-modes_mode_livescript"},"@codemirror/legacy-modes/mode/lua":{"file":"@codemirror_legacy-modes_mode_lua"},"@codemirror/legacy-modes/mode/mathematica":{"file":"@codemirror_legacy-modes_mode_mathematica"},"@codemirror/legacy-modes/mode/mbox":{"file":"@codemirror_legacy-modes_mode_mbox"},"@codemirror/legacy-modes/mode/mirc":{"file":"@codemirror_legacy-modes_mode_mirc"},"@codemirror/legacy-modes/mode/mllike":{"file":"@codemirror_legacy-modes_mode_mllike"},"@codemirror/legacy-modes/mode/modelica":{"file":"@codemirror_legacy-modes_mode_modelica"},"@codemirror/legacy-modes/mode/mscgen":{"file":"@codemirror_legacy-modes_mode_mscgen"},"@codemirror/legacy-modes/mode/mumps":{"file":"@codemirror_legacy-modes_mode_mumps"},"@codemirror/legacy-modes/mode/nginx":{"file":"@codemirror_legacy-modes_mode_nginx"},"@codemirror/legacy-modes/mode/nsis":{"file":"@codemirror_legacy-modes_mode_nsis"},"@codemirror/legacy-modes/mode/ntriples":{"file":"@codemirror_legacy-modes_mode_ntriples"},"@codemirror/legacy-modes/mode/octave":{"file":"@codemirror_legacy-modes_mode_octave"},"@codemirror/legacy-modes/mode/oz":{"file":"@codemirror_legacy-modes_mode_oz"},"@codemirror/legacy-modes/mode/pascal":{"file":"@codemirror_legacy-modes_mode_pascal"},"@codemirror/legacy-modes/mode/pegjs":{"file":"@codemirror_legacy-modes_mode_pegjs"},"@codemirror/legacy-modes/mode/perl":{"file":"@codemirror_legacy-modes_mode_perl"},"@codemirror/legacy-modes/mode/pig":{"file":"@codemirror_legacy-modes_mode_pig"},"@codemirror/legacy-modes/mode/powershell":{"file":"@codemirror_legacy-modes_mode_powershell"},"@codemirror/legacy-modes/mode/properties":{"file":"@codemirror_legacy-modes_mode_properties"},"@codemirror/legacy-modes/mode/protobuf":{"file":"@codemirror_legacy-modes_mode_protobuf"},"@codemirror/legacy-modes/mode/pug":{"file":"@codemirror_legacy-modes_mode_pug"},"@codemirror/legacy-modes/mode/puppet":{"file":"@codemirror_legacy-modes_mode_puppet"},"@codemirror/legacy-modes/mode/python":{"file":"@codemirror_legacy-modes_mode_python"},"@codemirror/legacy-modes/mode/q":{"file":"@codemirror_legacy-modes_mode_q"},"@codemirror/legacy-modes/mode/r":{"file":"@codemirror_legacy-modes_mode_r"},"@codemirror/legacy-modes/mode/rpm":{"file":"@codemirror_legacy-modes_mode_rpm"},"@codemirror/legacy-modes/mode/ruby":{"file":"@codemirror_legacy-modes_mode_ruby"},"@codemirror/legacy-modes/mode/rust":{"file":"@codemirror_legacy-modes_mode_rust"},"@codemirror/legacy-modes/mode/sas":{"file":"@codemirror_legacy-modes_mode_sas"},"@codemirror/legacy-modes/mode/sass":{"file":"@codemirror_legacy-modes_mode_sass"},"@codemirror/legacy-modes/mode/scheme":{"file":"@codemirror_legacy-modes_mode_scheme"},"@codemirror/legacy-modes/mode/shell":{"file":"@codemirror_legacy-modes_mode_shell"},"@codemirror/legacy-modes/mode/sieve":{"file":"@codemirror_legacy-modes_mode_sieve"},"@codemirror/legacy-modes/mode/simple-mode":{"file":"@codemirror_legacy-modes_mode_simple-mode"},"@codemirror/legacy-modes/mode/smalltalk":{"file":"@codemirror_legacy-modes_mode_smalltalk"},"@codemirror/legacy-modes/mode/solr":{"file":"@codemirror_legacy-modes_mode_solr"},"@codemirror/legacy-modes/mode/sparql":{"file":"@codemirror_legacy-modes_mode_sparql"},"@codemirror/legacy-modes/mode/spreadsheet":{"file":"@codemirror_legacy-modes_mode_spreadsheet"},"@codemirror/legacy-modes/mode/sql":{"file":"@codemirror_legacy-modes_mode_sql"},"@codemirror/legacy-modes/mode/stex":{"file":"@codemirror_legacy-modes_mode_stex"},"@codemirror/legacy-modes/mode/stylus":{"file":"@codemirror_legacy-modes_mode_stylus"},"@codemirror/legacy-modes/mode/swift":{"file":"@codemirror_legacy-modes_mode_swift"},"@codemirror/legacy-modes/mode/tcl":{"file":"@codemirror_legacy-modes_mode_tcl"},"@codemirror/legacy-modes/mode/textile":{"file":"@codemirror_legacy-modes_mode_textile"},"@codemirror/legacy-modes/mode/tiddlywiki":{"file":"@codemirror_legacy-modes_mode_tiddlywiki"},"@codemirror/legacy-modes/mode/tiki":{"file":"@codemirror_legacy-modes_mode_tiki"},"@codemirror/legacy-modes/mode/toml":{"file":"@codemirror_legacy-modes_mode_toml"},"@codemirror/legacy-modes/mode/troff":{"file":"@codemirror_legacy-modes_mode_troff"},"@codemirror/legacy-modes/mode/ttcn-cfg":{"file":"@codemirror_legacy-modes_mode_ttcn-cfg"},"@codemirror/legacy-modes/mode/ttcn":{"file":"@codemirror_legacy-modes_mode_ttcn"},"@codemirror/legacy-modes/mode/turtle":{"file":"@codemirror_legacy-modes_mode_turtle"},"@codemirror/legacy-modes/mode/vb":{"file":"@codemirror_legacy-modes_mode_vb"},"@codemirror/legacy-modes/mode/vbscript":{"file":"@codemirror_legacy-modes_mode_vbscript"},"@codemirror/legacy-modes/mode/velocity":{"file":"@codemirror_legacy-modes_mode_velocity"},"@codemirror/legacy-modes/mode/verilog":{"file":"@codemirror_legacy-modes_mode_verilog"},"@codemirror/legacy-modes/mode/vhdl":{"file":"@codemirror_legacy-modes_mode_vhdl"},"@codemirror/legacy-modes/mode/wast":{"file":"@codemirror_legacy-modes_mode_wast"},"@codemirror/legacy-modes/mode/webidl":{"file":"@codemirror_legacy-modes_mode_webidl"},"@codemirror/legacy-modes/mode/xml":{"file":"@codemirror_legacy-modes_mode_xml"},"@codemirror/legacy-modes/mode/xquery":{"file":"@codemirror_legacy-modes_mode_xquery"},"@codemirror/legacy-modes/mode/yacas":{"file":"@codemirror_legacy-modes_mode_yacas"},"@codemirror/legacy-modes/mode/yaml":{"file":"@codemirror_legacy-modes_mode_yaml"},"@codemirror/legacy-modes/mode/z80":{"file":"@codemirror_legacy-modes_mode_z80"},"@babel/runtime/helpers/interopRequireDefault":{"file":"@babel_runtime_helpers_interopRequireDefault"},"@babel/runtime/helpers/objectSpread2":{"file":"@babel_runtime_helpers_objectSpread2"},"@babel/runtime/helpers/toConsumableArray":{"file":"@babel_runtime_helpers_toConsumableArray"}};

// Base URL prepended to every module file fetch. Override via configure() before first load.
let _baseUrl = './codemirror/';

// Replaceable transport function. Receives the full URL, must return Promise<string> (JS source).
let _loader = (url) => fetch(url).then((r) => r.text());

/**
 * Configure the loader before any modules are requested.
 *
 * @param {object} options
 * @param {string} [options.baseUrl] - Directory URL where module files are served from.
 *   Trailing slash is added automatically. Defaults to './codemirror/'.
 * @param {(url: string) => Promise<string>} [options.loader] - Custom transport function.
 *   Replaces the default fetch-based loader. Useful for WebView asset registries,
 *   XMLHttpRequest, or any environment without fetch.
 */
function configure({ baseUrl, loader } = {}) {
  if (baseUrl !== undefined) {
    _baseUrl = baseUrl.endsWith('/') ? baseUrl : baseUrl + '/';
  }
  if (typeof loader === 'function') {
    _loader = loader;
  }
}

// Resolved exports for every module that has been loaded, keyed by package name.
// Stubs ({}) are inserted before loading begins so circular dependencies get the same reference.
const moduleCache = new Map();

// Tracks stubs that are still being populated (loading in progress).
const _pendingStubs = new Set();

// In-flight load Promises keyed by package name. Concurrent external callers for the
// same module await this Promise to get fully populated exports rather than the empty stub.
const _loadingPromises = new Map();

// Singleton promise for the core bundle load. All 14 core packages share one fetch.
let _coreBundlePromise = null;

/**
 * Fetches _core.js once and initialises all core packages in dependency order.
 * Each package's exports are merged into any pre-existing cache stub so that
 * callers that received a stub reference before the bundle finished still get
 * the populated object.
 */
function _loadCoreBundle() {
  if (_coreBundlePromise) return _coreBundlePromise;
  _coreBundlePromise = _loader(_baseUrl + '_core.js')
    .then(async (code) => {
      // eval returns the moduleInitFunction reference defined at the end of the bundle.
      // The trailing ';moduleInitFunction' is required because ESM is always strict —
      // strict-mode eval does not leak function declarations into the outer scope.
      const bundleInit = eval(code + ';moduleInitFunction');
      const bundleExports = await bundleInit(requireAsyncModule);
      for (const [name, exports] of Object.entries(bundleExports)) {
        const stub = moduleCache.get(name);
        if (stub) {
          // Populate the stub that was cached before the bundle loaded (covers
          // the case where multiple core modules were requested in parallel).
          Object.assign(stub, exports);
        } else {
          moduleCache.set(name, exports);
        }
      }
    });
  return _coreBundlePromise;
}

async function _asyncLoad(moduleName, moduleExports) {
  const info = modules[moduleName];
  if (info.bundle) {
    // Core module: load the shared bundle instead of an individual file.
    await _loadCoreBundle();
    const cached = moduleCache.get(moduleName);
    if (cached && cached !== moduleExports) Object.assign(moduleExports, cached);
    return moduleExports;
  }
  const code = await _loader(_baseUrl + info.file + '.js');
  const moduleInitFunction = eval(code + ';moduleInitFunction');
  const result = await moduleInitFunction(requireAsyncModule, moduleExports);
  // If the module replaced module.exports entirely (e.g. rollup CJS interop
  // assigns the default export), copy named exports back into the cached stub
  // so both existing stub references and future cache hits get the full object.
  if (result !== moduleExports && result instanceof Object) {
    Object.assign(moduleExports, result);
  }
  return moduleExports;
}

/**
 * Asynchronously loads a CodeMirror module by package name and returns its exports.
 * Results are cached — repeated calls for the same name return the cached value immediately.
 * A stub object is stored in the cache before loading begins, so circular dependencies
 * between modules receive the same reference that will be populated once loading completes.
 *
 * @param {string} moduleName - npm package name, e.g. '@codemirror/state'
 * @returns {object|Promise<object>} Cached exports (sync) or a Promise resolving to exports.
 */
function requireAsyncModule(moduleName) {
  if (moduleCache.has(moduleName)) {
    const cached = moduleCache.get(moduleName);
    if (_pendingStubs.has(cached)) return _loadingPromises.get(moduleName);
    return cached;
  }
  const moduleExports = {};
  _pendingStubs.add(moduleExports);
  moduleCache.set(moduleName, moduleExports);
  const promise = _asyncLoad(moduleName, moduleExports).then((result) => {
    _pendingStubs.delete(moduleExports);
    _loadingPromises.delete(moduleName);
    return result;
  });
  _loadingPromises.set(moduleName, promise);
  return promise;
}



/**
 * Registry mapping package name → (mod, options?) => CodeMirror extension.
 * Covers the extension-providing packages included in this distribution.
 * Add entries for custom packages with registerExtension().
 */
const extensionResolvers = new Map([
  ['@codemirror/autocomplete',   (mod, options) => mod.autocompletion(options)],
  ['@codemirror/search',         (mod, options) => mod.search(options)],
  ['@codemirror/lint',           (mod, options) => mod.lintGutter(options)],
  ['@codemirror/collab',         (mod, options) => mod.collab(options)],
  ['@codemirror/theme-one-dark', (mod) => mod.oneDark],
]);

/**
 * Register a resolver for a package not covered by the built-in registry,
 * or override an existing one.
 *
 * @param {string} moduleName - npm package name.
 * @param {(mod: object, options?: any) => import('@codemirror/state').Extension} resolver
 */
function registerExtension(moduleName, resolver) {
  extensionResolvers.set(moduleName, resolver);
}

/**
 * Resolves an extension spec into a CodeMirror Extension value.
 *
 * Accepted forms:
 *   - string                    → package name; loaded and resolved via extensionResolvers
 *   - [string, string]          → [packageName, exportName]; loads module, returns mod[exportName]
 *   - [string, object|undefined]→ package name + options object passed to the resolver
 *   - anything else             → returned as-is (already a CM Extension)
 */
async function resolveExtensionSpec(spec) {
  if (typeof spec !== 'string' && !(Array.isArray(spec) && typeof spec[0] === 'string')) {
    return spec;
  }
  const [moduleName, second] = Array.isArray(spec) ? spec : [spec, undefined];

  // [moduleName, 'exportName'] — return a named export directly (e.g. themes)
  if (typeof second === 'string') {
    const mod = await requireAsyncModule(moduleName);
    return mod[second];
  }

  const resolver = extensionResolvers.get(moduleName);
  if (!resolver) {
    throw new Error(`No extension resolver for "${moduleName}". Call registerExtension() to add one.`);
  }
  const mod = await requireAsyncModule(moduleName);
  return resolver(mod, second);
}

/**
 * Registry mapping language name → npm package name for packages that don't
 * follow the @codemirror/lang-{name} convention. Pre-populated with known
 * custom language packages bundled in this distribution.
 */
const languagePackages = new Map([
  ['sksl', '@actualwave/codemirror-lang-sksl'],
]);

/**
 * Register a custom language package that doesn't follow the
 * @codemirror/lang-{name} convention.
 *
 * @param {string} name - Language name used in createEditor({ language }).
 * @param {string} packageName - npm package name to load.
 */
function registerLanguage(name, packageName) {
  languagePackages.set(name, packageName);
}

/**
 * Extracts a StreamParser mode object from a legacy-modes module.
 * Tries exact match first, then case-insensitive, then single-export fallback
 * (handles mismatches like coffeescript→coffeeScript, simple-mode→simpleMode).
 */
function findLegacyMode(mod, name) {
  const isMode = (v) => v && typeof v === 'object' && !Array.isArray(v);
  const skip = new Set(['__esModule', 'default']);
  if (isMode(mod[name])) return mod[name];
  const lname = name.toLowerCase();
  for (const [k, v] of Object.entries(mod)) {
    if (!skip.has(k) && k.toLowerCase() === lname && isMode(v)) return v;
  }
  const modes = Object.entries(mod).filter(([k, v]) => !skip.has(k) && isMode(v));
  return modes.length === 1 ? modes[0][1] : null;
}

/**
 * Resolves a language name to a CodeMirror language extension.
 * Resolution order:
 *   1. Custom registry (registerLanguage / built-in overrides like sksl)
 *   2. @codemirror/lang-{name}  (official first-class language packages)
 *   3. @codemirror/legacy-modes/mode/{name}  (wrapped with StreamLanguage.define)
 */
async function resolveLanguageExtension(name) {
  if (languagePackages.has(name)) {
    const mod = await requireAsyncModule(languagePackages.get(name));
    const fn = mod[name] ?? Object.values(mod).find((v) => typeof v === 'function');
    if (fn) return fn();
  }

  try {
    const mod = await requireAsyncModule(`@codemirror/lang-${name}`);
    const fn = mod[name] ?? Object.values(mod).find((v) => typeof v === 'function');
    if (fn) return fn();
  } catch {}

  try {
    const [legacyMod, { StreamLanguage }] = await Promise.all([
      requireAsyncModule(`@codemirror/legacy-modes/mode/${name}`),
      requireAsyncModule('@codemirror/language'),
    ]);
    const mode = findLegacyMode(legacyMod, name);
    if (mode) return StreamLanguage.define(mode);
  } catch {}

  throw new Error(`No language support found for "${name}". Use registerLanguage() for custom packages.`);
}

/**
 * Creates and mounts a CodeMirror editor instance.
 * Core modules (state, view, basicSetup) are loaded on first call and cached for reuse.
 *
 * @param {object} [options]
 * @param {Element}  [options.parent=document.body] - DOM element to mount the editor into.
 * @param {string}   [options.doc='']               - Initial document content.
 * @param {string}   [options.language]             - Language name, e.g. 'javascript', 'python'.
 *   Tries @codemirror/lang-{name} first, then @codemirror/legacy-modes/mode/{name}.
 * @param {Array}    [options.extensions=[]]        - Extension specs. Each item may be:
 *   a package-name string, a [packageName, options] tuple, or an already-built CM Extension.
 * @param {(value: string) => void} [options.onChange] - Called with the full document string
 *   on every change. Use this to relay content back to the React Native side.
 *
 * @returns {Promise<EditorController>}
 */
async function createEditor({
  parent = document.body,
  doc = '',
  language,
  extensions = [],
  onChange,
} = {}) {
  const [
    { EditorView, lineNumbers, highlightActiveLineGutter, highlightSpecialChars,
      dropCursor, rectangularSelection, crosshairCursor, highlightActiveLine, keymap },
    { EditorState, Compartment },
    { history, defaultKeymap, historyKeymap },
    { foldGutter, indentOnInput, syntaxHighlighting, defaultHighlightStyle, bracketMatching, foldKeymap },
    { closeBrackets, autocompletion, closeBracketsKeymap, completionKeymap },
    { highlightSelectionMatches, searchKeymap },
    { lintKeymap },
  ] = await Promise.all([
    requireAsyncModule('@codemirror/view'),
    requireAsyncModule('@codemirror/state'),
    requireAsyncModule('@codemirror/commands'),
    requireAsyncModule('@codemirror/language'),
    requireAsyncModule('@codemirror/autocomplete'),
    requireAsyncModule('@codemirror/search'),
    requireAsyncModule('@codemirror/lint'),
  ]);

  // Chrome 126+ WebView uses the EditContext API for IME input. On Chrome 147 there is a
  // race condition where successive textupdate events arrive faster than CM6 can sync back
  // via editContext.updateText/updateSelection, causing characters to appear after the cursor.
  // Disabling EditContext makes CM6 fall back to the contenteditable MutationObserver path,
  // which is stable for fast typing when drawSelection() is omitted (native cursor stays visible).
  EditorView.EDIT_CONTEXT = false;

  // basicSetup without drawSelection() — drawSelection() hides the native browser cursor,
  // which breaks Android IME composition (ghost text and cursor not advancing when typing fast).
  const mobileSetup = [
    lineNumbers(),
    highlightActiveLineGutter(),
    highlightSpecialChars(),
    history(),
    foldGutter(),
    dropCursor(),
    EditorState.allowMultipleSelections.of(true),
    indentOnInput(),
    syntaxHighlighting(defaultHighlightStyle, { fallback: true }),
    bracketMatching(),
    closeBrackets(),
    autocompletion(),
    rectangularSelection(),
    crosshairCursor(),
    highlightActiveLine(),
    highlightSelectionMatches(),
    keymap.of([
      ...closeBracketsKeymap,
      ...defaultKeymap,
      ...searchKeymap,
      ...historyKeymap,
      ...foldKeymap,
      ...completionKeymap,
      ...lintKeymap,
    ]),
  ];

  // Separate Compartments allow language and extensions to be swapped independently
  // after the editor is created without rebuilding the entire editor state.
  const languageCompartment = new Compartment();
  const extensionCompartment = new Compartment();

  const [langExt, resolvedExtensions] = await Promise.all([
    language ? resolveLanguageExtension(language) : Promise.resolve([]),
    Promise.all(extensions.map(resolveExtensionSpec)),
  ]);

  const builtinExtensions = [
    mobileSetup,
    languageCompartment.of(langExt),
    extensionCompartment.of(resolvedExtensions),
  ];

  if (onChange) {
    builtinExtensions.push(
      EditorView.updateListener.of((update) => {
        if (update.docChanged) onChange(update.state.doc.toString());
      }),
    );
  }

  const view = new EditorView({
    state: EditorState.create({ doc, extensions: builtinExtensions }),
    parent,
  });

  /**
   * @typedef {object} EditorController
   */
  return {
    /** The underlying CodeMirror EditorView, for advanced direct access. */
    get view() { return view; },

    /** Returns the current document content as a string. */
    getValue() {
      return view.state.doc.toString();
    },

    /** Replaces the entire document content. */
    setValue(value) {
      view.dispatch({
        changes: { from: 0, to: view.state.doc.length, insert: value },
      });
    },

    /**
     * Switches the active language. Loads @codemirror/lang-{name} on demand.
     * @param {string} name - Language name, e.g. 'python', 'css'.
     */
    async setLanguage(name) {
      const ext = await resolveLanguageExtension(name);
      view.dispatch({ effects: languageCompartment.reconfigure(ext) });
    },

    /**
     * Replaces the active extension set. Accepts the same spec forms as createEditor's
     * extensions option: package-name strings, [name, options] tuples, or CM Extensions.
     * @param {Array} newExtensions
     */
    async setExtensions(newExtensions) {
      const resolved = await Promise.all(newExtensions.map(resolveExtensionSpec));
      view.dispatch({ effects: extensionCompartment.reconfigure(resolved) });
    },

    /**
     * Loads a module by package name and returns its raw exports.
     * Use when you need direct access to module internals not exposed through
     * the extension registry (e.g. building a custom completion source).
     * @param {string} moduleName
     * @returns {Promise<object>}
     */
    async loadExtension(moduleName) {
      return requireAsyncModule(moduleName);
    },

    /** Destroys the editor and removes it from the DOM. */
    destroy() {
      view.destroy();
    },
  };
}

global.CodeMirrorEditor = { configure, createEditor, requireAsyncModule, registerExtension };
})(typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : this);